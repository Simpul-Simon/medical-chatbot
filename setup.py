from setuptools import setup, find_packages

setup(
    name='generative_ai_project',  # Use a more concise name
    version='0.0.0',
    author='Simpul Simon',
    author_email='simpulsimonrout@gmail.com',
    packages=find_packages(),  # Automatically finds your packages
    install_requires=[  # If you have dependencies, list them here
        # Example:
        # 'requests', 
        # 'flask',
    ],
)
